import * as React from 'react';
import './TinyLoader.css';


const TinyLoader = () => {
  return (
    <div className={ `tiny-loader` }>
      <div className={ `tiny-loader__animation` }/>
    </div>
  );
};

export default TinyLoader;
